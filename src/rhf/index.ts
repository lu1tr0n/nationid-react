/**
 * `@nationid/react/rhf` — opt-in adapters for `react-hook-form` 7+. The
 * adapter wires a `<DocumentInput>` through `Controller`, exposes a typed
 * `useNationidController` helper, and registers a Zod-flavoured resolver
 * surface for callers that want to validate the whole form at submit time.
 *
 * `react-hook-form` is an optional peer dependency. Importing this subpath
 * without `react-hook-form` installed throws at module-load time.
 */
export {};
